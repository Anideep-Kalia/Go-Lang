// go mod init hello;
// go help

package main

import "fmt"

func main() {
	var username string = "anideep_kalia"
	var tru bool = false
	// uint not practical so we use int only
	var number int = 190
	// float64 and float32 for the decimla

	fmt.Println("Hello from ANIDEEP KALIA from past")
	fmt.Println(username)
	fmt.Printf("The datatype of username is %T \n", username)
	fmt.Println(tru)
	fmt.Println(number)

}

// go run main.go
